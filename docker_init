apt-get update

#install build essentials
apt-get install lsb-release default-jre wget ruby

#install locales
apt-get install locales
# Select en_US.UTF-8 in the dialog shown below
locale-gen en_US.UTF-8

#create workspace

mkdir -p /data/jenkins/workspace

